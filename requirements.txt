flask==3.0.3
flask-sqlalchemy==3.1.1
flask-login==0.6.3
flask-migrate==4.0.7
flask-wtf==1.2.1
pymysql==1.1.1
cryptography==42.0.8
werkzeug==3.0.3
ezdxf==1.3.4
weasyprint==62.3
Pillow>=11.0.0
# opencv-python-headless: no Python 3.14 wheels yet — install separately when needed (Phase 6)
python-dotenv==1.0.1
# 3D model export
# trimesh: powers GLB + STL 3D output and the web 3D viewer (pure python, works on 3.14)
trimesh==4.12.2
# cadquery: OpenCASCADE kernel — needed ONLY for STEP (.step) B-rep export.
# If pip fails on Python 3.14 (no cadquery-ocp wheels yet), skip it —
# GLB/STL/viewer still work via trimesh. Install when 3.14 wheels land, or use Python 3.12.
cadquery==2.8.0